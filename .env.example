NODE_ENV=production

# MongoDB Atlas SRV connection string, including the application database name.
# Keep the real username/password in .env only and URL-encode special characters.
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-host>/quill?retryWrites=true&w=majority

# JWT/session secrets should be long random strings, ideally 64+ characters.
JWT_SECRET=
SESSION_SECRET=
OTP_PEPPER=
COOKIE_SECURE=true

# Google OAuth credentials from Google Cloud Console.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Browser-facing URLs for the HTTPS backend and frontend.
SERVER_URL=https://localhost:5000
CLIENT_URL=https://localhost:3000

# Browser bundle API URL used by the Next.js frontend.
NEXT_PUBLIC_API_URL=https://localhost:5000/api

# Google reCAPTCHA keys. The NEXT_PUBLIC key is browser-safe; the secret is backend-only.
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=
RECAPTCHA_SECRET_KEY=
RECAPTCHA_EXPECTED_HOSTNAME=localhost
RECAPTCHA_MIN_SCORE=0

# Manual admin seed values. Keep these in .env only; never commit real admin credentials.
ADMIN_EMAIL=
ADMIN_USERNAME=admin
ADMIN_PASSWORD=
ADMIN_FULL_NAME=Quill Admin

# Email credentials for OTP and password reset delivery.
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM=noreply@quill.app

# OTP expiry window in minutes.
OTP_EXPIRY_MINUTES=10
